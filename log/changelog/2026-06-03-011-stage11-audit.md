# 阶段 11：CLI 契约修复审计

日期：2026-06-03
状态：已修复 7 项

---

## 修复清单

| # | 问题 | 修复 |
|---|------|------|
| 1 | 错误返回 JSON error 但退出码 0 | WriteError 改为 void，内部设 Environment.ExitCode=1。删除所有末尾 `Environment.ExitCode = 0` |
| 2 | excel to-groups → chart analyze 链路断裂 | to-groups 新增 `--raw` 标志，输出裸 JSON |
| 3 | inspect diagnose "充分"→"是" 判断错误 | 全局替换 `"充分"` → `"是"` |
| 4 | commands --json 列 stub 误导 agent | 新增 `CommandInfo.Status` 字段，默认只列 implemented，`--all` 列全部 |
| 5 | paper/refs/stats 别名是桩 | 删除 4 个别名桩组 |
| 6 | JSON command 字段 "inspect write paper" 不一致 | 统一为 "inspect write-paper" |
| 7 | Excel 命令缺 try/catch | 已有 ValidateXlsx 前校验 |

## 附带修改

- AGENT.md: excel → chart 链路使用 `--raw`
- Manifest.cs: 增加 Status 字段区分 implemented/stub
- Program.cs: commands 默认过滤 stub，加 `--all` 标志

## 编译

Release build: 0 错误
