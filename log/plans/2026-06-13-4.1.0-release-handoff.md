# Nong.Cli.Net 4.1.0 Release Handoff

日期: 2026-06-13
状态: done

## 施工结果

本 handoff 已执行为 4.1.0 release truth-source sync。

- `Cli/Common/CliVersion.cs` 已同步到 `4.1.0`。
- 当前真实命令发现为 `126 commands available`，OpenAI tools 也是 126；新增/纳入计数的 `progress report` 是主 CLI 真实轻模块命令。
- `README.md`、`README.zh-CN.md`、`CLAUDE.md`、`Cli/AGENT.md`、`Cli/README.md`、`docs/CAPABILITY.md`、`docs/release-checklist.md` 已同步到 4.1.0 模块化口径。
- 本轮验证：`dotnet test Cli.Tests\Cli.Tests.csproj -c Release --no-restore` -> 155 passed；`nong --version` -> `nong v4.1.0`；command discovery / OpenAI tools -> 126；clean temp pack audit -> `status: ok`, `packageCount: 7`。
- `log/plans/2026-06-13-modular-release-to-audit-roadmap.md` 状态已从 `pending` 收口为 `done`。
- 根目录 `nupkg/` 仍视为历史产物目录；发布前必须从当前项目文件打到干净输出目录再 audit。

下面的“原始发现”保留为施工输入记录；若与 `PROJECT_STATE.md` 或本节冲突，以当前 `PROJECT_STATE.md` 为准。

## 背景

这个文件是给新窗口继续施工用的单一交接点。先读本文件，再按需读它引用的 plan/changelog/guidance。

最近主线已经从 4.0.0 单体 CLI 推进到 4.1.0 模块化架构：

- 主 `nong` 变成轻路由器。
- `chart` / `diagram` / `pdf` / `pptx` / `ocr` / `imaging` 拆成独立 dotnet tool。
- 工具包 PackageId 已分离为 `Angri450.Nong.Tool.*`。
- `nong commands --json` 当前返回 126 条命令。
- `nong commands --format openai-tools` 已完成 NanoBot bridge Phase 1。

关键参考：

- `log/plans/2026-06-12-cli-modular-split.md`
- `log/plans/2026-06-13-modular-release-to-audit-roadmap.md`
- `log/plans/2026-06-12-ppocrv6-adaptation.md`
- `log/changelog/2026-06-13-ppocrv6-modular-split.md`
- `log/changelog/2026-06-13-modular-release-final-audit.md`
- `log/guidance/2026-06-13-thirdparty-boundary-audit.md`

## Git 状态

审计时状态：

```text
branch: main
remote: origin/main
state: ahead 6
worktree: clean
```

本地 `main` 比 `origin/main` 超前的 6 个提交：

```text
f7c205b docs: add full 16-package dependency graph to nong-status-4.1.0.html
842dd37 docs: update README.zh-CN to 4.1.0 modular architecture, add nong-status-4.1.0.html status panel
e3fad98 feat: NanoBot bridge Phase 1 - nong commands --format openai-tools (125 tools, 113 with param schemas)
699dde0 feat: stages 4-7 - ThirdParty audit, final release audit, changelog, local install closure
287ef84 feat: stages 2-3 - pack audit gate, RID volume slim (83MB->26MB), OCR PP-OCRv6 text cleanup
73eb44a feat: stage 1 - tool PackageId rename to Angri450.Nong.Tool.*, pack-audit script, ToolPackages constants, external tool routing
```

不要把这些本地提交当成已经同步到远端。

## 当前事实

当前项目文件已经是 4.1.0 / Tool package 架构：

```text
Cli/NongCli.csproj                Angri450.Nong.Cli          4.1.0  tool: nong
Chart/tools/nong-chart.csproj     Angri450.Nong.Tool.Chart   4.1.0  tool: nong-chart
Diagram/tools/nong-diagram.csproj Angri450.Nong.Tool.Diagram 4.1.0  tool: nong-diagram
Pdf/tools/nong-pdf.csproj         Angri450.Nong.Tool.Pdf     4.1.0  tool: nong-pdf
Pptx/tools/nong-pptx.csproj       Angri450.Nong.Tool.Pptx    4.1.0  tool: nong-pptx
MultiModal/tools/nong-ocr.csproj  Angri450.Nong.Tool.Ocr     4.1.0  tool: nong-ocr
Imaging/tools/nong-imaging.csproj Angri450.Nong.Tool.Imaging 4.1.0  tool: nong-imaging
```

主 CLI 仍内嵌轻模块：

```text
word / excel / genre / icons / literature / pandoc / inspect / skill / slice / progress / OcrModels
```

外部路由在 `Cli/Program.cs` 和 `Cli/Common/ToolPackages.cs`：

```text
chart   -> nong-chart   -> Angri450.Nong.Tool.Chart
diagram -> nong-diagram -> Angri450.Nong.Tool.Diagram
ocr     -> nong-ocr     -> Angri450.Nong.Tool.Ocr
pdf     -> nong-pdf     -> Angri450.Nong.Tool.Pdf
pptx    -> nong-pptx    -> Angri450.Nong.Tool.Pptx
word images/crop path -> nong-imaging -> Angri450.Nong.Tool.Imaging
```

OCR runtime pin is intentionally separate:

```text
Cli/Common/OcrRuntimeVersion.cs -> 4.0.0
```

Do not bump `OcrRuntimeVersion.Current` unless sibling repo `Nong.OcrRuntime` has published a new validated native runtime.

## Verification Already Run

Tests:

```powershell
dotnet test Cli.Tests\Cli.Tests.csproj -c Release --no-restore
```

Result:

```text
154 passed, 0 failed, 0 skipped
```

Current-project pack audit was run by packing the 7 release tools to a temp directory, then running:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\pack-audit.ps1 -Path <temp-pack-dir> -Json
```

Result:

```text
status: ok
packageCount: 7
Angri450.Nong.Cli           12.05 MB
Angri450.Nong.Tool.Chart    25.87 MB
Angri450.Nong.Tool.Diagram  25.87 MB
Angri450.Nong.Tool.Imaging  26.20 MB
Angri450.Nong.Tool.Ocr      12.15 MB
Angri450.Nong.Tool.Pdf      28.68 MB
Angri450.Nong.Tool.Pptx     10.74 MB
```

Important: `tools/pack-audit.ps1 -Path nupkg` currently fails because `nupkg/` contains stale old packages, including a 135 MB `Angri450.Nong.Cli.4.1.0.nupkg` from before modular slimming. That directory is not the current release output.

## Original Findings Before Construction

### 1. CLI version constant is stale

`Cli/NongCli.csproj` is `4.1.0`, but:

```text
Cli/Common/CliVersion.cs -> 4.0.0
```

Impact:

- `nong --version` prints `4.0.0`.
- JSON `meta.version` returns `4.0.0`.
- `nong commands --json` reports 125 commands but version `4.0.0`.

This is the first thing to fix.

### 2. English README is stale and ships to NuGet

`README.md` still says:

- one binary / 93 commands
- 4.0.0 release line
- PP-OCRv5 local OCR as the active wording
- current docs target `Angri450.Nong.Cli 4.0.0`

Many csproj files set:

```xml
<PackageReadmeFile>README.md</PackageReadmeFile>
```

Impact: publishing 4.1.0 now would put stale 4.0.0/PP-OCRv5 docs into NuGet package pages.

### 3. Agent and maintenance docs are stale

`CLAUDE.md` still says:

- published version `4.0.0`
- 14 main packages
- current line references `master`
- local OCR must be PP-OCRv5

`Cli/AGENT.md`, `docs/CAPABILITY.md`, and `docs/release-checklist.md` also contain old 4.0.0 / 93-command / PP-OCRv5 wording.

Impact: future agents will keep following outdated instructions.

### 4. `nupkg/` is stale

Root `nupkg/` currently contains old package outputs:

- old giant `Angri450.Nong.Cli.4.1.0.nupkg` around 135 MB
- old library packages like `Angri450.Nong.Chart.4.1.0.nupkg`
- no current `Angri450.Nong.Tool.*` packages

Impact:

- `tools/pack-audit.ps1 -Path nupkg` fails.
- `nupkg/` cannot be used for publish or local install tests until refreshed or moved aside.

### 5. Plan status is stale

`log/plans/2026-06-13-modular-release-to-audit-roadmap.md` still says:

```text
状态: pending
```

But the corresponding final audit says GO:

```text
log/changelog/2026-06-13-modular-release-final-audit.md
```

Impact: plan index/history is confusing.

## Recommended Construction Scope

Do one focused change: "4.1.0 release truth-source sync".

Suggested files:

```text
Cli/Common/CliVersion.cs
README.md
CLAUDE.md
Cli/AGENT.md
docs/CAPABILITY.md
docs/release-checklist.md
log/plans/2026-06-13-modular-release-to-audit-roadmap.md
log/plans/index.md
log/changelog/YYYY-MM-DD-4.1.0-release-doc-sync.md
log/changelog/index.md
```

Optional but recommended:

```text
nupkg/
```

For `nupkg/`, choose one of these:

1. Repack current 7 release tools into `nupkg/` after clearing stale 4.0.x / old 4.1.0 outputs.
2. Move stale output to an ignored archive outside the repo, then pack fresh outputs.
3. Leave `nupkg/` untouched but clearly document that release packing must use a clean output directory.

Do not delete or move `nupkg/` with a broad command unless the exact target paths are verified first.

## Suggested Edit Details

### `Cli/Common/CliVersion.cs`

Change:

```csharp
public const string Current = "4.0.0";
```

to:

```csharp
public const string Current = "4.1.0";
```

Do not change `Cli/Common/OcrRuntimeVersion.cs`.

### `README.md`

Bring it to the same 4.1.0 line as `README.zh-CN.md`:

- 126 commands.
- Modular architecture.
- Main `nong` is light router.
- External tools:
  - `Angri450.Nong.Tool.Chart`
  - `Angri450.Nong.Tool.Diagram`
  - `Angri450.Nong.Tool.Pdf`
  - `Angri450.Nong.Tool.Pptx`
  - `Angri450.Nong.Tool.Ocr`
  - `Angri450.Nong.Tool.Imaging`
- Local OCR default is PP-OCRv6:
  - `nong ocr install-model pp-ocrv6-medium --json`
- Keep OCR runtime repository wording:
  - `Angri450.Nong.OcrRuntime.*` stays separate and pinned independently.
- Mention Windows-only Skia/HarfBuzz asset strategy for Chart/Diagram/Imaging if documenting platform support.

### `CLAUDE.md`

Update current contract:

- published/current line: 4.1.0 modular release candidate/line, depending on publish state
- branch: `main`, not `master`
- package surface: 7 tool packages plus libraries as applicable
- local OCR: PP-OCRv6, with v5 retained as legacy compatibility
- keep "no Python core functionality"
- keep `Nong.OcrRuntime` as sibling runtime repo

### `Cli/AGENT.md`

Sync active command contract:

- 126 commands
- `ocr local` is PP-OCRv6-first, v5 legacy fallback if installed
- `ocr install-model` supports `pp-ocrv6`, `pp-ocrv6-medium`, `pp-ocrv6-small`, `pp-ocrv6-tiny`, and legacy `pp-ocrv5-mobile`
- `meta.version` examples should be 4.1.0 after `CliVersion.cs` changes
- add modular tool routing note for heavy modules

### `docs/CAPABILITY.md`

Either update to 4.1.0 / 126 commands or mark as historical 4.0.0 document. Do not leave it pretending to be current while showing 4.0.0.

### `docs/release-checklist.md`

Create/update 4.1.0 release checklist:

- 7 publishable tool packages:
  - `Angri450.Nong.Cli`
  - `Angri450.Nong.Tool.Chart`
  - `Angri450.Nong.Tool.Diagram`
  - `Angri450.Nong.Tool.Pdf`
  - `Angri450.Nong.Tool.Pptx`
  - `Angri450.Nong.Tool.Ocr`
  - `Angri450.Nong.Tool.Imaging`
- `CliVersion.Current == 4.1.0`
- `nong commands --json` reports 126 commands and version 4.1.0
- `tools/pack-audit.ps1` must pass on a clean pack output dir
- root `nupkg/` must not contain stale publish candidates
- no NuGet publish unless explicitly requested

## Verification Required After Construction

Run:

```powershell
dotnet test Cli.Tests\Cli.Tests.csproj -c Release --no-restore
```

Confirm:

```powershell
.\Cli\bin\Release\net8.0\nong.exe --version
.\Cli\bin\Release\net8.0\nong.exe commands --json
.\Cli\bin\Release\net8.0\nong.exe commands --format openai-tools
```

Expected:

```text
nong v4.1.0
126 commands available
meta.version = 4.1.0
openai tools count = 126
```

Pack into a clean temp dir:

```powershell
$out = Join-Path $env:TEMP ("nong-pack-audit-" + [guid]::NewGuid().ToString("N"))
New-Item -ItemType Directory -Path $out | Out-Null
dotnet pack Cli\NongCli.csproj -c Release --no-restore -o $out
dotnet pack Chart\tools\nong-chart.csproj -c Release --no-restore -o $out
dotnet pack Diagram\tools\nong-diagram.csproj -c Release --no-restore -o $out
dotnet pack Pdf\tools\nong-pdf.csproj -c Release --no-restore -o $out
dotnet pack Pptx\tools\nong-pptx.csproj -c Release --no-restore -o $out
dotnet pack MultiModal\tools\nong-ocr.csproj -c Release --no-restore -o $out
dotnet pack Imaging\tools\nong-imaging.csproj -c Release --no-restore -o $out
powershell -NoProfile -ExecutionPolicy Bypass -File tools\pack-audit.ps1 -Path $out -Json
```

Expected:

```text
status = ok
packageCount = 7
all package IDs use Angri450.Nong.Cli or Angri450.Nong.Tool.*
```

## Stop Conditions

Stop and report instead of continuing if:

- tests fail;
- `nong --version` still reports 4.0.0 after rebuild;
- pack output still creates old tool package IDs like `Angri450.Nong.Chart` for dotnet tools;
- pack audit fails on a clean output directory;
- root `nupkg/` cleanup would require deleting/moving uncertain paths.

## Non-Goals

- Do not publish to NuGet.
- Do not push GitHub/Gitee/GitCode.
- Do not change `OcrRuntimeVersion.Current`.
- Do not merge `Nong.OcrRuntime` back into this repo.
- Do not split `ThirdParty` in this pass.
- Do not change user command names.
