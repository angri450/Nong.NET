# 4.1.0 local install release-candidate closure

日期: 2026-06-13
状态: done

## Result

4.1.0 local install release-candidate closure passed.

- Baseline tests passed: `155 passed, 0 failed, 0 skipped`.
- Clean pack audit passed: `status: ok`, `packageCount: 7`.
- All 7 packages installed from the clean local source into a fresh temp `--tool-path`.
- Installed tool list:
  - `angri450.nong.cli` `4.1.0` -> `nong`
  - `angri450.nong.tool.chart` `4.1.0` -> `nong-chart`
  - `angri450.nong.tool.diagram` `4.1.0` -> `nong-diagram`
  - `angri450.nong.tool.imaging` `4.1.0` -> `nong-imaging`
  - `angri450.nong.tool.ocr` `4.1.0` -> `nong-ocr`
  - `angri450.nong.tool.pdf` `4.1.0` -> `nong-pdf`
  - `angri450.nong.tool.pptx` `4.1.0` -> `nong-pptx`
- Installed `nong --version` returned `nong v4.1.0`.
- Installed `nong commands --json` returned `126 commands available`, `meta.version = 4.1.0`.
- Installed `nong commands --format openai-tools` emitted 126 tool schemas.
- Direct external tool help passed for `nong-chart`, `nong-diagram`, `nong-pdf`, `nong-pptx`, `nong-ocr`, and `nong-imaging`.
- Direct and routed JSON missing-file smokes returned structured `E001` for chart and PDF:
  - `nong-chart anova nonexistent.json --json`
  - `nong chart anova nonexistent.json --json`
  - `nong-pdf check nonexistent.pdf --json`
  - `nong pdf check nonexistent.pdf --json`

Root `nupkg/` was intentionally left untouched and remains non-authoritative.

## Current-State Files

- `PROJECT_STATE.md`
- `CLAUDE.md`
- `AGENTS.md`
- Previous handoff: `log/plans/2026-06-13-4.1.0-release-handoff.md`
- Release checklist: `docs/release-checklist.md`

## Goal

Validate the 4.1.0 modular package set through a real local dotnet-tool install loop before any publish decision.

This pass must answer:

- Can the 7 current 4.1.0 tool packages be packed from current project files into a clean source?
- Can the main CLI and external tools install from that clean local source into a fresh `--tool-path`?
- Does the installed `nong` expose the expected 4.1.0 / 126-command contract?
- Do installed external tool commands start and expose help or JSON-compatible failure paths?
- What is the current policy for root `nupkg/`?

## Scope

- Pack these 7 projects to a clean temp package source:
  - `Cli/NongCli.csproj`
  - `Chart/tools/nong-chart.csproj`
  - `Diagram/tools/nong-diagram.csproj`
  - `Pdf/tools/nong-pdf.csproj`
  - `Pptx/tools/nong-pptx.csproj`
  - `MultiModal/tools/nong-ocr.csproj`
  - `Imaging/tools/nong-imaging.csproj`
- Install the 7 packages into a fresh temp `--tool-path` from that local source.
- Run smoke checks from the installed tools.
- Record results in changelog and update this plan status.

## Verification Commands

Required baseline:

```powershell
dotnet test Cli.Tests\Cli.Tests.csproj -c Release --no-restore
```

Pack/audit:

```powershell
dotnet pack <7 projects> -c Release --no-restore -o <clean-pack-dir>
powershell -NoProfile -ExecutionPolicy Bypass -File tools\pack-audit.ps1 -Path <clean-pack-dir> -Json
```

Install/smoke:

```powershell
dotnet tool install Angri450.Nong.Cli --tool-path <clean-tool-path> --add-source <clean-pack-dir> --ignore-failed-sources
dotnet tool install Angri450.Nong.Tool.Chart --tool-path <clean-tool-path> --add-source <clean-pack-dir> --ignore-failed-sources
dotnet tool install Angri450.Nong.Tool.Diagram --tool-path <clean-tool-path> --add-source <clean-pack-dir> --ignore-failed-sources
dotnet tool install Angri450.Nong.Tool.Pdf --tool-path <clean-tool-path> --add-source <clean-pack-dir> --ignore-failed-sources
dotnet tool install Angri450.Nong.Tool.Pptx --tool-path <clean-tool-path> --add-source <clean-pack-dir> --ignore-failed-sources
dotnet tool install Angri450.Nong.Tool.Ocr --tool-path <clean-tool-path> --add-source <clean-pack-dir> --ignore-failed-sources
dotnet tool install Angri450.Nong.Tool.Imaging --tool-path <clean-tool-path> --add-source <clean-pack-dir> --ignore-failed-sources
```

Expected smoke:

- `<tool-path>\nong.exe --version` -> `nong v4.1.0`
- `<tool-path>\nong.exe commands --json` -> `126 commands available`, `meta.version = 4.1.0`
- `<tool-path>\nong.exe commands --format openai-tools` -> 126 tool schemas
- Direct external tools start:
  - `nong-chart --help`
  - `nong-diagram --help`
  - `nong-pdf --help`
  - `nong-pptx --help`
  - `nong-ocr --help`
  - `nong-imaging --help`
- At least one JSON missing-file path from an installed external tool returns structured `E001`.

## nupkg Strategy

Root `nupkg/` is not a release source in this pass. Leave it untouched unless exact stale paths are reviewed and a separate cleanup decision is made.

Any publish candidate must come from a clean pack directory created during the release pass.

## Non-Goals

- Do not publish NuGet packages.
- Do not push GitHub/Gitee/GitCode.
- Do not clean root `nupkg/` destructively.
- Do not change user command names.
- Do not change `Cli/Common/OcrRuntimeVersion.cs`.
- Do not add native render tests to the default test suite.

## Stop Conditions

- `dotnet test` fails.
- Pack audit fails.
- Local tool install fails for current 4.1.0 packages.
- Installed `nong` reports anything other than `nong v4.1.0`.
- Installed command discovery reports anything other than 126 tools.
- A missing-file JSON smoke path emits non-JSON output or lacks `E001`.
